## 3. Configurable at run-time.

You shall make your application configurable at run-time regardless of where it is deployed (dev/staging/production)
12-Factor App methodology: your application should simply expect environment variables to be there, regardless of how they got there.
