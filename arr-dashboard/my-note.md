I have tried without success to make nix work with docker.
I so much lack nix basics for some reason. I keep forgetting how to setup flakes! but it doesn't matter since you could just ask AI to do it!.

I want to explore next with systemd-nspawn instead of docker. the goal is, launch opencode, create a development environment for AI Agents that's secure and air-gapped!.

My next goal is, finding a way to share secrets without the AI being able to read it at all. but to be able to use these secrets in running the application and reading it's outputs.

nixpkgs opencode doesn't work for some odd reason.
