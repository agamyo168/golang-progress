version: "3"
services:
  db:
    image: postgres
    environment:
      POSTGRES_DB: ecomm_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: adminpassword
    volumes:
      - ecomm_postgres_volume:/var/lib/postgresql/data
    ports:
      - "5432:5432"
volumes:
  ecomm_postgres_volume:
